"""
處理程序模組初始化（Phase C 後）

業務 handlers 全砍 — 業務邏輯走 rewrite/。剩下純 admin / framework 工具：
  database_sync_handler / sync_router / sync_handler / sequence_fix_handler /
  cleanup_handler / diagnosis_handler / location_message_handler /
  image_message_handler
"""
