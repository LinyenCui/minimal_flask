# 未來時間態 (Future Time Mode) 使用指南

## 概述
未來時間態專門處理未來的班次規劃和預約，包括固定班次的匯入管理。本系統採用陽曆周次（星期日至星期六）的循環週期，確保班次規劃的準確性和一致性。

## 核心功能

### 1. 固定班次匯入系統
支援自然語言周次參數，提供智能化的班次規劃工具。

#### 基本命令格式
```
匯入固定班次 [周次] [覆蓋]
```

#### 周次參數支援
- `本周` / `這周` → 當前周次
- `下周` / `下一周` → 下一周次
- `上周` / `上一周` → ❌ 拒絕（過去時間態保護）

#### 覆蓋選項（可選）
- `覆蓋` → 覆蓋已存在的該周次固定班次

### 2. 使用範例

#### 基本匯入
```
匯入固定班次              # 顯示可用選項
匯入固定班次 本周         # 匯入本周固定班次
匯入固定班次 下周         # 匯入下周固定班次
```

#### 覆蓋模式匯入
```
匯入固定班次 本周 覆蓋    # 覆蓋本周已存在的固定班次
匯入固定班次 下周 覆蓋    # 覆蓋下周已存在的固定班次
```

### 3. 重複檢查機制

當檢測到已匯入過該周次的固定班次時：
```
⚠️ 本周 (7/13-7/19) 的固定班次已經匯入過了（共 133 筆）。

如需覆蓋，請使用：
🔄 匯入固定班次 本周 覆蓋

⚠️ 注意：如選覆蓋資料，原先對班次的修改會失效
```

### 4. 操作模式

#### 本周模式（追加模式）
- 在現有班次基礎上追加新班次
- 自動檢查重複，避免重複匯入
- 適合補充遺漏的班次

#### 下周模式（規劃模式）
- 如果沒有覆蓋選項，會自動將未完成班次移至已完成表
- 清空現有班次後匯入新周次
- 適合全新周次的規劃

### 5. 安全機制

#### 過去時間態保護
```
匯入固定班次 上周         # ❌ 拒絕操作
> 不允許匯入過去時間態：上周 (2025-01-19 至 2025-01-25)
```

#### 覆蓋保護
- 只有明確指定「覆蓋」選項才會替換現有班次
- 覆蓋會導致原先的班次修改失效
- 提供詳細的警告信息

### 6. 周次計算邏輯

#### 陽曆周次系統
- 採用國際標準：星期日為一周開始
- 周次循環：星期日(0) → 星期六(6)
- 確保跨月份周次計算正確

#### 周次邊界處理
- 自動處理跨月份的周次
- 智能識別周次邊界
- 防止周次重疊或遺漏

## 技術實現

### 1. 周次計算模組
位置：`modules/utils/week_utils.py`

主要功能：
- 陽曆周次計算
- 自然語言參數解析
- 過去時間態保護
- 周次邊界處理

### 2. 匯入處理器
位置：`modules/handlers/import_handler.py`

主要功能：
- 命令解析和驗證
- 周次計算和驗證
- 清理功能執行
- 固定班次匯入
- 狀態統計和報告

### 3. 清理機制
- 智能清理選項
- 安全的資料刪除
- 詳細的清理統計
- 錯誤處理和回滾

## 最佳實踐

### 1. 周次規劃建議
- 建議在周六或周日進行下周規劃
- 使用「清理已完成」選項定期清理歷史資料
- 避免在周中大量修改固定班次

### 2. 清理策略
- 每周匯入時使用「清理已完成」保持資料庫整潔
- 需要重新規劃時使用「清理全部」
- 定期備份重要資料

### 3. 錯誤預防
- 確認周次參數正確
- 謹慎使用清理選項
- 留意系統提示訊息
- 定期檢查匯入結果

## 未來發展方向

### 1. 智能化增強
- 更多自然語言支援
- 預測性班次規劃
- 自動化清理建議

### 2. 整合性提升
- 與現在時間態無縫整合
- 跨時間態資料同步
- 統一的操作界面

### 3. 報告功能
- 匯入統計報告
- 清理效果分析
- 班次規劃建議

---

**注意事項：**
1. 未來時間態系統設計為防止過去時間態操作
2. 清理功能不可逆，請謹慎使用
3. 建議定期使用「清理已完成」保持系統效能
4. 如有疑問，請使用「匯入固定班次」查看可用選項 